{
	instances = {
		skeleton = Template(f"skeleton.html") {
			title->Return("title") {
				default->Write("Untitled");
			};
			style->Return("style") {
				default->Write("");
			};
			content->Return("content") {
				default->Write("Lorem ipsum");				
			};
			script->Return("script") {
				default->Write("");
			};
		};

		list = Template(f"list.html") {
			items->Return("items") {
				default->Write("<li class=\"empty\">Empty</li>");
			};
		};

		item = Template(f"item.html") {
			body->Return("body") {
				default->Write(variablename = "value");
			};
		};	
	};
}